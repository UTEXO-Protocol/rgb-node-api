[package]
edition = "2024"
name = "wallet-cli"
version = "0.1.0"
description = "Dev-only key generation and PSBT signing helper for manual rgb-node-api testing"
publish = false

[dependencies]
anyhow = "1.0.81"
clap = { version = "4.5.4", features = ["derive", "env"] }
rgb-lib.workspace = true
serde = { version = "1.0.197", features = ["derive"] }
serde_json = "1.0.117"
