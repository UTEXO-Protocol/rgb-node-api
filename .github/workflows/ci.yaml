name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"
  # Keep the intentional sibling path patch reproducible on fresh runners.
  RGB_LIB_REV: "3828a59affb528884daf77a68005194e78fb9c81"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: rustfmt + clippy
    runs-on: ubuntu-latest
    env:
      BFA_MIRRORS_TOKEN: ${{ secrets.BFA_MIRRORS_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Prepare pinned RGB dependencies
        run: bash .github/scripts/prepare-rgb-deps.sh
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy
          cache: true
      - name: cargo fmt --check
        run: cargo fmt --all -- --check
      - name: cargo clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

  check-and-test:
    name: cargo check + test
    runs-on: ubuntu-latest
    env:
      BFA_MIRRORS_TOKEN: ${{ secrets.BFA_MIRRORS_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Prepare pinned RGB dependencies
        run: bash .github/scripts/prepare-rgb-deps.sh
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - name: cargo check
        run: cargo check --workspace --all-targets --locked
      - name: cargo test
        run: cargo test --workspace --all-targets --locked

  docker:
    name: build + push image
    needs: [lint, check-and-test]
    if: >-
      github.event_name == 'push' &&
      (github.ref == 'refs/heads/main' ||
       startsWith(github.ref, 'refs/tags/v'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so build.rs resolves a real commit/branch, not a shallow sha.
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # `:main` for branch builds; `:vX.Y.Z` + `:vX.Y` + `:latest` for tags;
      # always the short sha for traceability.
      - name: Image tags & labels
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/utexo-protocol/rgb-node-api
          tags: |
            type=ref,event=branch
            type=sha,prefix=,format=short
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
